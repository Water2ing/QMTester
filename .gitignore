# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# ── Virtual environments (recreate per README's reproduction steps) ──────────
.venv_local/
.venv_cirq/
.venv/
venv/

# ── Editor / OS / tooling ────────────────────────────────────────────────────
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── Third-party / network-downloaded corpora and benchmarks (not re-uploaded) ─
LintQ/
platforms_oopsla22/
vendor/
benchmarks/
curation/corpus/

# ── Large binaries that exceed GitHub's 100 MB file limit ────────────────────
containers/*.sif
# Per-pair raw logs (~134 MB each); regenerate from shards via derive_metrics.py
**/raw_runs.jsonl

# ── Scratch run/build logs ───────────────────────────────────────────────────
*.log

# ── Bulky regenerable raw per-pair logs (keep the small summary CSVs alongside) ─
data/results/runs/rich_noise_v1/falsepos_rich_noisy.jsonl
data/results/runs/rich_noise_v1/falsepos_rich_noisy_cal.jsonl

# ── Stale top-level circuit-level results (superseded by paper_ready/ + frozen run) ─
data/results/shards/
data/results/canonical_results.csv

# ── LaTeX build artifacts (regenerated by pdflatex/bibtex) ───────────────────
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
paper/*.toc
paper/QMTester.pdf

# ── Review / inspection scratch material ─────────────────────────────────────
paper/docs/
paper/IEEE-conference-template-062824/

# ── Empty placeholder dir ────────────────────────────────────────────────────
files/
